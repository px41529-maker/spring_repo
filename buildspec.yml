version: 0.2

phases:
  build:
    commands:
      - echo "Build Started"
      - mvn clean package

  post_build:
    commands:
      - echo "Build Completed Successfully"

artifacts:
  files:
    - target/app.jar
  discard-paths: yes